export function colorBars(datum) {
  let returner;

//   datum.x === 'Green' && returner = 'green';

  if (datum.x === 'Green') returner = 'green';

  console.log(returner);

  return returner;
    

    // datum.x === 'Green' ? 'green' : datum.x === 'Pink' ? 'pink' : datum.x === 'Yellow' ? 'yellow' : datum.x === 'Puce' ? '#CC8899' : datum.x === 'Turquoise' ? 'turquoise' : datum.x === 'Violet' ? 'violet' : datum.x === 'Goldenrod' ? 'goldenrod' : datum.x === 'Blue' ? 'blue' : datum.x === 'Purple' ? 'purple' : datum.x === 'Khaki' ? 'khaki' : datum.x === 'Fuscia' ? '#D9027D' : datum.x === 'Aquamarine' ? 'aquamarine' : datum.x === 'Maroon' ? 'maroon' : datum.x === 'Mauv' ? '#BB85AB' : datum.x === 'Indigo' ? 'indigo' : datum.x === 'Orange' ? 'orange' : datum.x === 'Teal' ? 'teal' : datum.x === 'Crimson' ? 'crimson' : datum.x === 'Red' ? 'red' : '',

}